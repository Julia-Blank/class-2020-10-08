---
title: "demo"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r}
#data <- tibble(col = c(1, 1, 2, 2, 3, 3), col2 = c("hi", "hi", "low", "high", "low", "low")) %>%
  #group_by(col, col2) %>%
  #summarize(countcol = n(), .groups = "drop")

#data


```

